spring:
  application:
    name: lija
  datasource:
    generate-unique-name: false
    name: lija
  mvc:
    throw-exception-if-no-handler-found: true
    static-path-pattern: /assets/**

redis:
  host: localhost